<thlogic>
    <attr sel="#headId" th:replace="~{fragments/head::headFragment}"/>
    <attr sel="#play" th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}" th:text="#{game.play}"/>
    <attr sel="#game-welcome" th:text="#{game.welcome}"/>
    <attr sel="#footer-id" th:replace="~{fragments/footer::footerFragment}"/>

</thlogic>